[project]
name = "autodocgen-frontend"
version = "0.1.0"
description = "AutoDocGen frontend - Flask web interface"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask",
    "requests",
    "python-dotenv",
    "markdown",
]
